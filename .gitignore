# Build output
.gradle/
build/
out/

# Isolated dev RuneLite config dir — used by `gradle run` to avoid mixing the
# dev test client with the user's real ~/.runelite config. Holds the test
# client's account state, plugin settings, and OSRS cache.
.dev-home/

# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Eclipse
.project
.settings/
.classpath

# NetBeans
nbactions.xml
nb-configuration.xml
nbproject/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Local config / secrets (never commit)
local.properties
gradle.properties.local
.env

# Local dev scripts (not part of the plugin)
build07flip.bat
run07flip.bat
run07flip.ps1

# AI assistant files (not part of the plugin)
CLAUDE.md
claude.md
build-guide.md
.claude/
memory/
docs/

# Git worktrees (local dev only)
.worktrees/

# Image source files / scratch (PNG exports are committed, sources stay local)
*.psd
images/Untitled-*
